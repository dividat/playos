#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

mypy kiosk_browser
# TODO: temporarily ignored, pytest produces a non-zero exit code, since kiosk
# has no unit tests at the moment :-)
python -m pytest -v || true
